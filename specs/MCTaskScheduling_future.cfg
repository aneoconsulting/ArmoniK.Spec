CONSTANTS
    AgentId = {a, b, c}
    ObjectId = {o, p, q}
    TaskId = {t, u, v}
    SUBMITTED = SUBMITTED
    CREATED = CREATED
    STARTED = STARTED
    COMPLETED = COMPLETED
    LOCKED = LOCKED
    NULL = NULL
    Next <- MCNext

SPECIFICATION
    Spec

INVARIANTS
    TypeInv

PROPERTIES
    ImplementsSimpleTaskScheduling
    ImplementsSimpleObjectProcessing

\* SYMMETRY
\*     Symmetry
