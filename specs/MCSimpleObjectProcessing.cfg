CONSTANTS
    \* Object identifiers (model values)
    ObjectId = {o, p, q}
    
    \* Object status values (model values)
    NULL = NULL
    CREATED = CREATED
    COMPLETED = COMPLETED
    LOCKED = LOCKED


SPECIFICATION
    MCSpec


INVARIANTS
    TypeInv


PROPERTIES
    EventualCompletion
    Quiescence
