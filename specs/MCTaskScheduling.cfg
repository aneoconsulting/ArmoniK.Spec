CONSTANTS
    AgentId = {a, b}
    ObjectId = {o, p, q}
    TaskId = {t, u}
    SUBMITTED = SUBMITTED
    CREATED = CREATED
    STARTED = STARTED
    COMPLETED = COMPLETED
    LOCKED = LOCKED
    NULL = NULL
    Next <- MCNext

SPECIFICATION
    Spec

INVARIANTS
    TypeInv
    AllTasksHaveIO
    UniqueObjectOwner
    AllInputsLocked
    AllOutputsCompleted

PROPERTIES
    ImplementsSimpleTaskScheduling
    ImplementsSimpleObjectProcessing
