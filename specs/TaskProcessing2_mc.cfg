CONSTANTS
    \* Agent identifiers (model values)
    AgentId = {a, b}

    \* Task identifiers (model values)
    TaskId = {t, u, v}

    \* NULL is defined as an empty string
    NULL = ""

    MaxRetries = 2
    IsDenumerableSet <- IsFiniteSet

SPECIFICATION
    Spec

INVARIANTS
    TypeInv
    TaskStateIntegrity

PROPERTY
    PermanentFinalization
    FailedTaskEventualRetry
    EventualFinalization
    RefineTP1

ACTION_CONSTRAINT 
    ActionConstraint
